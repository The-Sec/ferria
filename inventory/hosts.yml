---
all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: "{{ ansible_playbook_python }}"
  children:
    # Define your machine groups below.
    # Machines self-select based on hostname — add a host to a group to apply
    # that group's roles and group_vars to it.
    #
    # Example:
    # webservers:
    #   hosts:
    #     web01:
    #     web02:
    # databases:
    #   hosts:
    #     db01:
    homelab:
      hosts:
        localhost:
